I connect tout30

R clish -c "show web session-timeout" V> %%WEB_TIMEOUT%% tout30 
& BBP:S >:S #:S

L echo %%WEB_TIMEOUT%% | awk '{print $2}' V> %%WEB_TIMEOUT%%

I trim %%WEB_TIMEOUT%% V> %%WEB_TIMEOUT%% tout30

L if (%%WEB_TIMEOUT%% less 10) echo "WebUI timeout is set > 10 minutes" 
* F:WebUI timeout is set > 10 minutes

L if (%%IDDQD%% isnotempty) echo "WebUI timeout is set <= 10 minutes" 
* S:WebUI timeout is set <= 10 minutes